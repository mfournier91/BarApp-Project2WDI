<% @bars.each do |bar| %>
<div class="bar">
  <%= image_tag(bar.img_url) %>
  <h3><a href="<%= bar_path(bar) %>"><%= bar.name %></a></h3>
  <h4><%= bar.address %></h4>
</div>
<% end %>
<h5><%= link_to 'Add a new bar', new_bar_path %></h5>
